---
system_chart_repo_name: system-charts
system_chart_repo_path: /epirepo/helm-charts/system
# Use custom repository or use ssh tunnel for Helm
system_chart_repo_url: "{{ (custom_repository_url | default('http://localhost', true)) ~ system_chart_repo_path }}"
